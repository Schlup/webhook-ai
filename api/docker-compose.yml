services:
    postgres:
        image: postgres:17
        environment:
            POSTGRES_USER: docker
            POSTGRES_PASSWORD: docker
            POSTGRES_DB: webhooks
        ports:
            - "5433:5432"
